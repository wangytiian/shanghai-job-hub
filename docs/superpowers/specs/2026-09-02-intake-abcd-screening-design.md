# 招聘入库 A/B/C/D 分级设计

抓取文本先经硬规则，再由 AI 给出建议等级。A 为优先入库，B 为普通入库，C 为人工复核，D 为过滤留档。硬规则命中体检、面试、录用、拟录取、入职、收费招聘、高职称、负责人、三年以上经验或年龄偏高等明确不适配信号时直接归 D，并保存命中的原文片段。

AI 仅在没有硬拦截时，基于标题与原文输出等级、理由、证据和置信度。A/B 进入待核验；C 不自动分发；D 不进入岗位中心但保留审计记录。AI 不可用时默认 C，绝不自动丢弃招聘线索。
